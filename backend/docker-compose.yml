version: '3.8'

services:
  backend:
    build: .
    container_name: uniconnect-backend
    env_file:
      - .env
    ports:
      - "5501:5501"
    # No local database service needed; backend connects to Supabase via environment variables
