version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5502:5502"
    environment:
      # Add any required environment variables here
      - NODE_ENV=production
    restart: unless-stopped
